# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database
MONGODB_URI=mongodb+srv://admindb:261XNZwH5H4ytwdG@palhands.rtxny5x.mongodb.net/palhands_test?retryWrites=true&w=majority&appName=PalHands

# Test JWT Configuration
JWT_SECRET=test-jwt-secret-key-2024
JWT_EXPIRES_IN=1h

# Test Payment Configuration
PAYMENT_CASH_ENABLED=true
PAYMENT_STRIPE_ENABLED=false
PAYMENT_PAYPAL_ENABLED=false
PAYMENT_AUDIT_ENABLED=true

# Test Webhook Configuration
WEBHOOK_MAX_AGE_MINUTES=5
WEBHOOK_REPLAY_PROTECTION_ENABLED=true

# Test Outbox Configuration
OUTBOX_PENDING_INTERVAL_MS=1000
OUTBOX_RETRY_INTERVAL_MS=5000
OUTBOX_CLEANUP_INTERVAL_MS=60000
OUTBOX_BATCH_SIZE=10
OUTBOX_MAX_ATTEMPTS=2
OUTBOX_CLEANUP_DAYS=1

# Test Reconciliation Configuration
RECONCILIATION_DAILY_INTERVAL_MS=60000
RECONCILIATION_WEEKLY_INTERVAL_MS=120000
RECONCILIATION_MONTHLY_INTERVAL_MS=180000
RECONCILIATION_BATCH_SIZE=2
RECONCILIATION_MAX_RETRIES=2
RECONCILIATION_DISCREPANCY_THRESHOLD=0.01

# Disable email sending in tests
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=

# Test App Configuration
APP_BASE_URL=http://localhost:3001
CORS_ORIGIN=http://localhost:3001
