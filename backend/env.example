# Essential Environment Variables for PalHands Backend
# Copy this file to .env and update the values as needed

# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
MONGODB_URI=mongodb+srv://admindb:261XNZwH5H4ytwdG@palhands.rtxny5x.mongodb.net/palhands?retryWrites=true&w=majority&appName=PalHands

# JWT Configuration
JWT_SECRET=palhands-jwt-secret-key-2024-change-in-production
JWT_EXPIRES_IN=7d

# Email Configuration (Optional - for email verification)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=PalHands Support <support@palhands.com>

# Frontend Base URL used for building password reset links
# Example (Flutter web dev): http://localhost:8080
APP_BASE_URL=http://localhost:8080

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# Security Configuration
BCRYPT_ROUNDS=12
CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# Feature Flags
ENABLE_EMAIL_VERIFICATION=true
ENABLE_DEBUG_MODE=true

# Payment Configuration
PAYMENT_CASH_ENABLED=true
PAYMENT_STRIPE_ENABLED=false
PAYMENT_PAYPAL_ENABLED=false
PAYMENT_AUDIT_ENABLED=true

# Stripe Configuration (if PAYMENT_STRIPE_ENABLED=true)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# PayPal Configuration (if PAYMENT_PAYPAL_ENABLED=true)
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here
PAYPAL_WEBHOOK_SECRET=your_paypal_webhook_secret_here

# Webhook Configuration
WEBHOOK_MAX_AGE_MINUTES=5
WEBHOOK_REPLAY_PROTECTION_ENABLED=true

# Outbox Configuration
OUTBOX_PENDING_INTERVAL_MS=5000
OUTBOX_RETRY_INTERVAL_MS=30000
OUTBOX_CLEANUP_INTERVAL_MS=3600000
OUTBOX_BATCH_SIZE=50
OUTBOX_MAX_ATTEMPTS=3
OUTBOX_CLEANUP_DAYS=30

# Reconciliation Configuration
RECONCILIATION_DAILY_INTERVAL_MS=86400000
RECONCILIATION_WEEKLY_INTERVAL_MS=604800000
RECONCILIATION_MONTHLY_INTERVAL_MS=2592000000
RECONCILIATION_BATCH_SIZE=5
RECONCILIATION_MAX_RETRIES=3
RECONCILIATION_DISCREPANCY_THRESHOLD=0.01